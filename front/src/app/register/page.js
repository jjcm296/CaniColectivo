import RegisterForm from '@/features/auth/components/register/register-forms/RegisterForm';

export default function RegisterPage() {
    return (
        <main className="page-container">
            <RegisterForm />
        </main>
    );
}
